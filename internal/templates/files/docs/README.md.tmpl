# {{.Project.Name}}

{{if .Project.Description}}{{.Project.Description}}{{else}}GitOps repository for managing infrastructure and applications.{{end}}

## Overview

| Property | Value |
|----------|-------|
| Platform | {{.Platform}} |
| Scope | {{.Scope}} |
| GitOps Tool | {{.GitOpsTool}} |
| Environments | {{range $i, $e := .Environments}}{{if $i}}, {{end}}{{$e.Name}}{{end}} |

## Structure

```
{{.Project.Name}}/
├── bootstrap/          # GitOps tool installation
{{if or (eq .Scope "infrastructure") (eq .Scope "both")}}├── infrastructure/     # Cluster resources
{{end}}{{if or (eq .Scope "application") (eq .Scope "both")}}├── applications/       # Application deployments
{{end}}├── {{.GitOpsTool}}/             # GitOps configurations
└── docs/               # Documentation
```

## Quick Start

### Prerequisites

- kubectl configured for target cluster
- {{if eq .GitOpsTool "argocd"}}ArgoCD CLI{{else if eq .GitOpsTool "flux"}}Flux CLI{{else}}ArgoCD and Flux CLIs{{end}}

### Bootstrap

```bash
# Apply bootstrap manifests
kubectl apply -k bootstrap/{{.GitOpsTool}}/

# Verify installation
{{if eq .GitOpsTool "argocd"}}argocd app list{{else}}flux get all{{end}}
```

## Environments

{{range .Environments}}
### {{.Name}}

{{if .Cluster}}- Cluster: `{{.Cluster}}`{{end}}
- Path: `*/overlays/{{.Name}}/`
{{end}}

## Generated by gitopsi

This repository was bootstrapped using [gitopsi](https://github.com/ihsanmokhlisse/gitopsi).
