apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
spec:
  interval: {{ .Interval }}
  url: {{ .URL }}
  ref:
    branch: {{ .Branch }}
{{- if .SecretRef }}
  secretRef:
    name: {{ .SecretRef }}
{{- end }}
