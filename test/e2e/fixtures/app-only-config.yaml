project:
  name: test-app-only
  description: Application-only test project

platform: kubernetes
scope: application
gitops_tool: argocd
preset: standard

environments:
  - name: dev
  - name: staging
  - name: prod

docs:
  readme: true
  architecture: true
  onboarding: true

git:
  url: https://github.com/test/test-app-only.git

output:
  type: local
